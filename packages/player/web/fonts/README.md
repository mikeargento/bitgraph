JetBrains Mono, latin subset, variable weight 100-800: the site's data font
(website/src/app/layout.tsx loads it through next/font/google). Copied from
the site's build output so verify.html can embed it as a data URI and render
hashes, positions, and time windows exactly as the proof page does, offline.
Licensed under the SIL Open Font License 1.1 (embedding permitted).
